# Claude Code 2.1.81 Fingerprint

**Extracted**: 2026-03-21
**Source**: `@anthropic-ai/claude-code@2.1.81` npm package (`cli.js`)
**Previous version**: 2.1.80

## Version-Specific Values

| Field                 | Value         |
| --------------------- | ------------- |
| CLI version           | `2.1.81`      |
| Anthropic SDK version | `0.74.0`      |
| Axios version         | `1.13.6`      |
| Default timeout       | `600` seconds |

## OAuth Scopes

Two scope arrays merged for authorization:

```
["org:create_api_key", "user:profile"]                          // API key scopes
["user:profile", "user:inference", "user:sessions:claude_code",  // Auth scopes
 "user:mcp_servers", "user:file_upload"]
```

Full scope string: `org:create_api_key user:profile user:inference user:sessions:claude_code user:mcp_servers user:file_upload`

## Beta Flags

### Always enabled

- `claude-code-20250219`
- `oauth-2025-04-20`

### Conditionally enabled

| Beta                              | Condition                              |
| --------------------------------- | -------------------------------------- |
| `interleaved-thinking-2025-05-14` | Model supports thinking                |
| `context-1m-2025-08-07`           | 1M context model                       |
| `context-management-2025-06-27`   | Non-interactive + flags                |
| `structured-outputs-2025-12-15`   | Supports + `TENGU_TOOL_PEAR`           |
| `tool-examples-2025-10-29`        | Non-interactive + `TENGU_SCARF_COFFEE` |
| `effort-2025-11-24`               | Opus 4.6 models                        |
| `fast-mode-2026-02-01`            | Always (2.1.79+ base)                  |
| `advanced-tool-use-2025-11-20`    | Always (2.1.79+ base)                  |
| `prompt-caching-scope-2026-01-05` | Non-interactive, skip round-robin      |
| `token-counting-2024-11-01`       | `/v1/messages/count_tokens` endpoint   |
| `web-search-2025-03-05`           | vertex/foundry + supported model       |
| `files-api-2025-04-14`            | `/v1/files` or body has `file_id`      |
| `ccr-triggers-2026-01-30`         | RemoteTriggerTool (channels) only      |

### BEDROCK_UNSUPPORTED (exact 4)

- `interleaved-thinking-2025-05-14`
- `context-1m-2025-08-07`
- `tool-search-tool-2025-10-19`
- `tool-examples-2025-10-29`

### Defined but not auto-included

- `redact-thinking-2026-02-12` (set to empty string `""` since 2.1.80)
- `afk-mode-2026-01-31`
- `tool-search-tool-2025-10-19`

## HTTP Headers

### API Request User-Agent

```
claude-cli/2.1.81 (external, <entrypoint>[, agent-sdk/<v>][, client-app/<a>][, workload/<w>])
```

### OAuth Token User-Agent

```
axios/1.13.6
```

(Axios default — no explicit UA set on token exchange/refresh)

### Stainless Headers

| Header                        | Value                                |
| ----------------------------- | ------------------------------------ |
| `x-stainless-lang`            | `js`                                 |
| `x-stainless-package-version` | `0.74.0`                             |
| `x-stainless-os`              | `MacOS` / `Windows` / `Linux` / etc. |
| `x-stainless-arch`            | `x64` / `arm64` / etc.               |
| `x-stainless-runtime`         | `node`                               |
| `x-stainless-runtime-version` | `process.version`                    |
| `x-stainless-helper-method`   | `stream`                             |
| `x-stainless-timeout`         | `600`                                |
| `x-stainless-retry-count`     | `0` (first request)                  |

## Billing Header

```
x-anthropic-billing-header: cc_version=2.1.81.<hash>; cc_entrypoint=cli; cch=00000;
```

- `cch`: Fixed `00000`
- Version hash: `SHA-256("59cf53e54c78" + chars_at_positions_4_7_20_from_first_user_message).hex().slice(0,3)`

## Changes from 2.1.80

Bugfix-heavy release with minimal fingerprint changes:

1. Version strings bumped: `2.1.80` → `2.1.81`
2. New tool-level beta `ccr-triggers-2026-01-30` for `RemoteTriggerTool` (channels feature, not in main beta builder)
3. Fixed `CLAUDE_CODE_DISABLE_EXPERIMENTAL_BETAS` not suppressing structured-outputs beta in Stainless SDK `.parse()` method
4. Added `--bare` flag for scripted `-p` calls
5. Added `--channels` permission relay
6. Fixed concurrent OAuth session token refresh race condition
7. Updated MCP OAuth to support CIMD / SEP-991

OAuth flow, endpoints, scopes, client IDs, PKCE — all identical between versions.
