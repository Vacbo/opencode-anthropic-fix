# Claude Code 2.1.107 Fingerprint

**Extracted**: 2026-04-14
**Source**: `@anthropic-ai/claude-code@2.1.107` npm package (`cli.js`)
**Previous runtime baseline**: 2.1.98

## Verified Changes from 2.1.98

The fixed `cc-version-tracker` skill scripts and the stronger `scripts/analysis/*` pipeline agree on one substantive extracted change between `2.1.98` and `2.1.107`:

1. CLI version strings move from `2.1.98` to `2.1.107`
2. `managed-agents-2026-04-01` appears in the extracted beta set

## Verified Stable Extracted Values

These extracted values remained unchanged across the `2.1.98 -> 2.1.107` comparison:

- **Production client ID**: `9d1c250a-e61b-44d9-88ed-5944d1962f5e`
- **Staging client ID**: `22422756-60c9-4084-8eb7-27705fd5cf9a`
- **OAuth beta**: `oauth-2025-04-20`
- **OAuth scopes**:
    - `org:create_api_key`
    - `user:file_upload`
    - `user:inference`
    - `user:mcp_servers`
    - `user:profile`
    - `user:sessions:claude_code`
- **Token endpoint marker**: `/oauth/token`
- **Billing placeholder**: `cch=00000`

## Beta Notes

### Newly observed in the extracted baseline

- `managed-agents-2026-04-01`

### Still present in the extracted baseline

- `advisor-tool-2026-03-01`
- `advanced-tool-use-2025-11-20`
- `prompt-caching-scope-2026-01-05`
- `task-budgets-2026-03-13`

## Alignment Notes for This Repository

The plugin now pins `FALLBACK_CLAUDE_CLI_VERSION` to `2.1.107`, but standard `/v1/messages` parity follows the live Proxyman capture instead of the extracted bundle-only beta set. That means `advisor-tool-2026-03-01` and `managed-agents-2026-04-01` remain upstream-known betas, but are not auto-included on the main message path.

The request-header parity path now also emits `X-Claude-Code-Session-Id` from the plugin's per-session UUID, matching the direct 2.1.107 bundle evidence that API requests carry both a stable session header and a per-request `x-client-request-id`.

The stronger analysis extractor still reports some low-signal bundle-noise differences (`x-stainless-helper` minified fragments and an extra extracted hash-position slice), but live request capture now takes precedence for message-path beta composition.
